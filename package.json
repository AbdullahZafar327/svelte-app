{
  "name": "spa-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"vite\"",
    "server": "npm --prefix backend run start",
    "build": "vite build",
    "preview": "vite preview"
  },
  "workspaces": [
    "backend"
  ],
  "dependencies": {
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "concurrently": "^9.0.0",
    "svelte": "^5.55.5",
    "vite": "^8.0.12"
  }
}
